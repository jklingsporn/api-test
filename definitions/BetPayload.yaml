type: "object"
properties:
  amount:
    type: "integer"
    format: "int64"
    description: "The amount subtracted from the wallet. Needs to be positive"
  virtualAmount:
    type: "integer"
    format: "int64"
    description: "Freespins. For later use."